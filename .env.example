# Copy to `.env` and adjust. Real env vars (e.g. docker -e) override these.

# Shard/worker count (rounded up to a power of two). Defaults to CPU count.
WORKERS=4

# Where each shard persists its cool.log + backup.log. Defaults to ./data
# DATA_DIR=data

# Total memory budget in MB across all shards (split evenly per shard). When a
# shard hits 90% of its slice, LRU evicts down to 80%. Unset / 0 = unlimited.
# MAX_MEMORY_MB=1024

# Auth: set BOTH to require credentials on handshake. Leave unset = auth off.
DATASET_USERNAME=admin
DATASET_PASSWORD=secret

# Log level: error | warn | info | debug | trace
RUST_LOG=info
